// backend/generator/steps/fetchContent.js
export default async function fetchContent({ niche, city, competitors = '' }) {
  /* placeholder – returns empty arrays until we add Puppeteer scrape */
  return { competitorImages: [], competitorSnippets: [] };
}
