// Placeholder—ready to wire in PhantomBuster later
export default async function phantomScraper(keyword, location) {
  console.warn('PhantomBuster scraper stub called – returning empty set');
  return [];
}
